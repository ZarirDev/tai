package main

import (
	"testing"
)

// TestMainExists ensures the main package compiles and has no obvious issues.
func TestMainExists(t *testing.T) {
	// This is a placeholder – it forces the compiler to parse the main package.
	// Real tests live in the sub‑packages (groq, pkg/…, cmd).
}
